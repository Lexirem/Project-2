{{!-- aqui va la lista de las photos deseadas --}}


{{!-- indicar el total de la suma de los objetos --}}

{{!-- aqui va un boton de volver --}}
{{!-- y un boton que redirige a Payments --}}

<h3>This is your cart</h3>

<h1>{{this.item.title}}</h1>
<form id="shopping-cart" action="/add-to-cart" method="GET">

    <table class="shopping-cart">
        <thead>
            <tr>
                <th scope="col">Item</th>
                <th scope="col">Qty</th>
                <th scope="col">Price</th>
            </tr>
        </thead>
        <tbody>
            <tr>{{#each cart}}
                <th>{{cart.image}}</th>
                <th>{{cart.item.qty}}</th>
                <th>{{this.price}}</th>
            {{/each}}</tr>
        </tbody>
    </table>

    
    <p id="sub-total">
        <strong>Sub Total</strong>: <span id="stotal"></span>
    </p>
    <ul id="shopping-cart-actions">
        <li>
            <input type="submit" name="update" id="update-cart" class="btn" value="Update Cart" />
        </li>
        <li>
            <input type="submit" name="delete" id="empty-cart" class="btn" value="Empty Cart" />
        </li>
        <li>
            <a href="/themes" class="btn">Continue Shopping</a>
        </li>
        <li>
            <a href="/payments" class="btn">Go To Payment</a>
        </li>
    </ul>
</form>